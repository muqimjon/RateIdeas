namespace RateIdeas.Domain.Constants;

public abstract class Roles
{
    public const string User = nameof(User);
    public const string Admin = nameof(Admin);
    public const string SuperAdmin = nameof(SuperAdmin);
}